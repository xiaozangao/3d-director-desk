# 目标 7：群友预设独立入口与贡献元数据

完成日期：2026-07-17

## 本阶段完成

- 运镜工作台的“一键镜头”新增“基础预设 / 群友预设”两个独立入口，用户不再把官方基础动作与群友方案混在一起。
- 基础预设保留推、拉、左右摇、俯仰、左右移镜。
- 群友预设当前包含环绕摇臂升镜、跟拍、平行跟拍、手持晃镜、过肩绕正面、近景半环绕、环绕摇臂降镜、低机位追拍、俯视跟拍和横移揭示。
- 两组继续共用跟踪主体和实时轨迹范围，不复制生成逻辑，也不改变现有轨迹数据结构。
- 每个预设现在保存：名称、功能说明、适用目标、版本和所属分组。
- 群友预设额外保存贡献者昵称、联系方式、来源链接和许可说明字段。
- 已根据用户提供的截图确认贡献者为 `AIGC 耀光`，抖音号为 `AIJPDM001`。
- 选中群友预设后直接显示版本、说明、适用目标、贡献者和许可；有联系方式或来源时才显示对应内容。
- 首页和 README 已增加群友贡献名单，展示姓名、贡献内容和抖音号。

## 自动验证

- 底层验证官方与群友预设分组稳定，群友预设均有完整贡献元数据结构。
- UI 验证两个入口互斥显示，切到群友预设后可套用跟拍并跟踪当前人物。
- 聚焦测试：2 个文件、23 项通过。
- TypeScript：`npx tsc -b --pretty false` 通过。
- `git diff --check` 通过。

## 浏览器验证

- 隔离实例：`community_preset_verify`，未使用正式导演台。
- “群友预设”按钮 `aria-pressed=true`。
- 套用跟拍后生成 3 个可编辑镜头轨迹点。
- 页面显示 `跟拍 · v1.0.0`、适用目标、贡献者 `AIGC 耀光`、抖音号 `AIJPDM001` 和许可说明。
- 页面横向溢出为 0，业务控制台错误为 0；只有浏览器图标请求产生的 404。
- 截图：`../assets/smoke-results/community-presets-smoke-result.png`。

## 贡献资料维护规则

- 当前已确认贡献者：`AIGC 耀光`，抖音号 `AIJPDM001`。
- 新贡献者的昵称、联系方式、来源链接和展示许可仍必须由用户提供并确认。
- 首页、README 和预设元数据需要同步更新，避免三处信息不一致。
- 当前未提交、未推送、未更新在线版。
