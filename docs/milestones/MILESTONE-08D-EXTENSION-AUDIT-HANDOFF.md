# 目标 8D：二创扩展接口严格收口交接

完成日期：2026-07-17

## 最终结论

目标 8 已在本地完成严格收口。协议保持只读工程与受限结果回传，不内置 ComfyUI、LLM 或远程场景修改。当前修改未提交、未推送、未同步 GitHub 或在线版。

## 本轮补齐

- 工程文档建立显式逐级迁移管线：旧裸工程按版本 0 输入，经 `0 -> 1` 迁移；不修改源对象。
- 严格拒绝字符串、`null`、缺失、负数、非整数和未来文档版本。
- 工程快照测试覆盖镜头路线/FOV、逐点目标、身体部位、柔和跟随、人物路线、动作、停留和自定义曲线。
- 本地可移植性同时覆盖模型素材与动作素材。
- 首帧、当前帧和尾帧均有宿主桥自动测试与真实 iframe PNG 像素验收。
- 并发媒体导出返回 `export-busy`；错误 origin 和错误父窗口 source 均拒绝。
- 宿主 React 示例校验 origin、source、协议版本、requestId 和 action；视频超时为 60 秒，回调重渲染不再中断在途请求。

## 真实浏览器证据

### 三帧与插件

- 页面：`/extension-protocol-smoke.html`
- 首帧、当前 42%、尾帧均为 1280 像素宽非黑 PNG。
- 首尾归一化帧差：`0.03642`。
- 导出前后均为 42% / 3.36 秒 / 暂停 / 导演视角 / `cam_1`。
- 插件结果 1 条，`stale: false`。
- 证据图：`../assets/smoke-results/extension-protocol-smoke-result-v2.png`。

### WebM

- 页面：`/extension-video-smoke.html`
- 首轮严格真实时间录制发现最前约 0.1 秒黑帧，已修复。
- 修复后：8 秒、VP9、`621,328` bytes、1280 x 633。
- 0.1 秒首帧亮度 `0.037133`、方差 `0.010747`。
- 5.6 秒后段亮度 `0.079405`、方差 `0.014062`。
- 帧差 `0.086529`，两帧均非黑且视频不是静止画面。
- 导出后时间轴恢复到原来的 42%。
- 证据图：`../assets/smoke-results/extension-video-smoke-result-v2.png`。

## 自动验证

- 全仓：85 个测试文件、665 项通过，0 失败、0 跳过。
- `npm run build`：通过。
- `git diff --check`：通过。
- 构建仅保留既有大 chunk 提示；高斯实验分包约 4.97 MB，与目标 8 无功能冲突。

## 外部待办

- Windows 真机 FPS 和配置建议仍需群友匿名报告，不能在 macOS 上伪造。
- 群友贡献榜仍需用户提供确认后的昵称、联系方式、来源和展示许可。
- 目标 8 v1 按设计不自动应用插件结果；未来若增加写入，必须另设显式权限、预览差异和用户确认。
