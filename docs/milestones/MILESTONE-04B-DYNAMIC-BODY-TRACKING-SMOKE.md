# 目标 4B：动态身体部位跟拍五视图验收

完成日期：2026-07-17

## 验收范围

- 真实人物：`camille.fbx`。
- 真实动作：`wave.fbx`。
- 跟拍部位：右手。
- 视图：导演主视口、成片监看小窗、第一视角、看成片、视频导出。
- 时间点：0%、25%、50%、75%、100%。
- 生命周期：倒拖 50%、重播 0%、重播 50%。

## 自动验收逻辑

- 每个 Canvas 都从执行动作后的真实场景骨骼读取头、腰、左右手、左右脚世界坐标。
- 每个 Canvas 独立调用正式的 `getRuntimeCameraPlaybackSnapshot`，计算当前摄影机跟拍目标。
- 摄影机跟拍目标必须等于当前右手骨骼世界坐标，容差 `0.0005`。
- 五个视图的骨骼姿势和跟拍目标必须彼此一致。
- 五个时间点的右手跟拍目标必须产生实际移动，不能只是固定高度或人物中心。
- 倒拖和重播后，人物姿势及跟拍目标必须回到首次采样的确定性结果。

## 浏览器结果

- 状态：`passed`。
- 动作总变化量：`0.572547`。
- 右手跟拍目标总移动量：`0.572547`。
- 五个时间点跨视图骨骼最大差：全部 `0`。
- 五个时间点跨视图跟拍目标最大差：全部 `0`。
- 五个时间点跟拍目标到右手骨骼误差：全部 `0`。
- 倒拖与两次重播的骨骼基线误差：全部 `0`。
- 倒拖与两次重播的跟拍目标基线误差：全部 `0`。
- 浏览器业务错误：`0`。

## 自动验证

- 聚焦测试：3 个文件、11 项通过。
- 全仓测试：80 个文件、627 项通过。
- `npm run build`：通过，仅有既有大 chunk 警告。
- `git diff --check`：通过。

## 保留状态

- 未提交、未推送、未同步 GitHub 或在线版。
- 未清空浏览器导演台数据，未覆盖模型资源。
- 未处理 `.firecrawl/`、`.research-video/`、`.video-build/`、`成片/`。
